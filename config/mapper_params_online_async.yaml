slam_toolbox:
  ros__parameters:
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan
    mode: mapping

    resolution: 0.05
    minimum_range: 0.2             # RPLIDAR A1 minimum sensing range
    max_laser_range: 12.0          # RPLIDAR A1 rated range
    minimum_time_interval: 0.2
    transform_publish_period: 0.02
    map_update_interval: 2.0
    transform_timeout: 0.3

    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.2
    minimum_travel_heading: 0.2

    # Odometry here is wheel-only (no IMU), so it drifts more under wheel
    # slip than scan matching's default search window assumes. Widen the
    # window so scan matching can still find the correct alignment even
    # when odom's predicted pose is further off than usual.
    correlation_search_space_dimension: 1.0
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.15
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 12.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
